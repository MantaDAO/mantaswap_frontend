import BaseCard from '@/app/components/cards/Base';

const VaultCard = () => {
  return (
    <BaseCard
      type={5}
      className="w-full h-[130px]"
    ></BaseCard>
  );
};

export default VaultCard;
